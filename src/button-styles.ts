import { css } from "lit";

export const buttonStyles = css`
  button {
    color: inherit;
    font-family: inherit;
    font-size: inherit;
    background-color: #ab99af;
    border-radius: 5px;
    border: none;
    padding: 0.2em 0.5em;
    cursor: pointer;
  }
`;
